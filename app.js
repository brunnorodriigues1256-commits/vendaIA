const sections=document.querySelectorAll('.s');document.querySelectorAll('[data-s]').forEach(b=>b.onclick=()=>{sections.forEach(s=>s.classList.toggle('on',s.id===b.dataset.s));scrollTo(0,0)});let n=+(localStorage.getItem('vendaia_n')??5);remaining.textContent=n;generate.onclick=async()=>{const product=document.getElementById('product').value.trim(),audience=document.getElementById('audience').value.trim(),objective=document.getElementById('objective').value;if(!product||!audience){status.textContent='Preencha os dois campos.';return}if(n<=0){status.textContent='Limite grátis atingido.';return}generate.disabled=true;generate.textContent='⏳ GERANDO...';status.textContent='A IA está criando seu anúncio...';result.classList.remove('show');try{const r=await fetch('/.netlify/functions/generate-ad',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({product,audience,objective})});const d=await r.json();if(!r.ok)throw Error(d.error||'Erro ao gerar');result.textContent=d.text;result.classList.add('show');n--;localStorage.setItem('vendaia_n',n);remaining.textContent=n;status.textContent='Anúncio gerado com sucesso. ✅'}catch(e){status.textContent='Erro: '+e.message}finally{generate.disabled=false;generate.textContent='🚀 GERAR ANÚNCIO'}};